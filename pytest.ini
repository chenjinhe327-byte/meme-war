[pytest]
testpaths = tests/direct
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
markers =
    direct: tests that execute contracts in the in-memory direct runner
    glsim: tests that need the glsim consensus simulator
